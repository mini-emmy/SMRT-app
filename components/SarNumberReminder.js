import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SarNumberReminder = props => {
    <View><Text></Text><Button /></View>

    return;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  }
}
);

export default SarNumberReminder; 

